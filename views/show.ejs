<div class="container">
    <div class="row post-head">
        <h1 class="col-12 post-title"><%= postData.title %></h1>
        <p class="col-12 post-author fs-4">Posted by  ---> get user data back and print name here</p>
    </div>
    <p class="fs-5"><%= postData.content%></p>

    <div>
        <h3>Comments</h3>
    </div>

    <div>
        <% allComments.forEach(comment => { %>
                <%= comment.content %>
        <% }) %>
    </div>
</div>

